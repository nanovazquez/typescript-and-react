export interface IUser {
  id: string;
  name: string;
  initials: string;
}

export interface IPage {
  url: string;
  title: string;
}

export interface IApplication {
  id: string;
  name: string;
  target: string;
  status: string;
}
